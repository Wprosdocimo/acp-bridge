# Changelog

| Version | Date | Description |
|---------|------|-------------|
| v0.44.0 | 2026-08-18 | Pipeline convergence loop (loop-until-converged): a `context.next` carrying a `when` threshold expression turns the auto-chain into a bounded retry edge — after a completed pipeline, the Bridge reads a machine-readable verdict from `shared_cwd`, evaluates the threshold, and either converges (`when` False), re-submits the tail from `loop_back_to` reusing the same `shared_cwd` with the round counter incremented (`when` True and rounds remain), or stops at `max_rounds`. Motivating shape: the make-game QA loop ("QA fails → opengame fixes → kiro redeploys → re-QA"), which the five single-pass modes and the unconditional `context.next` chain could not express (JSON cannot self-reference). `when` is evaluated by an **AST whitelist** (comparisons, `and`/`or`/`not`, attribute access, numeric/string literals, whitelisted names) — **never `eval()`**: no `Call`/`Subscript`/comprehension/import/dunder-access, verified by injection-rejection tests. Fail-safe throughout — a missing metric makes its comparison False, and a missing/unparseable verdict or an unevaluable condition **stops** the loop (does not burn rounds) with a webhook noting why; only `completed` pipelines are evaluated, so a genuine failure (agent crash, missing declared artifact) takes the normal `failed` path and never loops. Each round re-submits as a fresh `sequence` pipeline (own id, linked via `next_pipeline_id`); step prompts reference `{{_loop_round}}` (0 on first pass) to read the previous round's report. **Template support** (explicit requirement): the entire loop def survives `POST /pipelines` `input`/`vars` rendering — `{{threshold}}`/`{{rounds}}` placeholders in `when`/`max_rounds` render at submit time, while `{{_loop_round}}` is a reserved runtime var (`_RUNTIME_VARS` in `src/render.py`) left verbatim so it neither 400s as "missing" at submit nor leaks a literal placeholder (filled per round by the runtime render). Recovery-safe: restart restores `context` (incl. `next`/`_loop_round`), so an interrupted loop child re-evaluates its threshold on resume. Protected-file change (human-approved): `pipeline.py` — `import ast`, `_eval_condition`/`_check_cond_ast`/`_resolve_cond`, `_read_verdict`, `_maybe_loop`, `_loop_webhook`, `_run` seeds `_loop_round` and dispatches `next.when` to the loop vs. the existing `_auto_chain` (plain `next` behavior byte-identical). Self-review caught 3 bugs before commit: `_artifacts` is step-indexed so the re-run tail slices `arts[back_to:]`; the child pipeline *is* the tail so its `loop_back_to` is rewritten to 0 (else the fix step falls out after round 1); and the child's `next` def is copied (`{**loop_def, ...}`) to avoid mutating the parent's shared context. New coverage: `test/unit/test_pipeline_loop.py` (29 cases: eval-condition + injection rejection, verdict read/missing/bad-json, the five `_maybe_loop` outcomes, multi-round tail stability, template preservation/runtime-round). Not yet end-to-end: harness-factory's game-qa still emits only markdown; the Bridge side is complete and unit-covered, real convergence data waits on a `verdict.json` producer. |
| v0.43.1 | 2026-08-15 | S3 uploads carry correct Content-Type. All three upload paths (`s3.upload`, `upload_bytes`, `put_bytes`) previously stored objects as boto3's default `binary/octet-stream`, so presigned GET links served zips/images/videos with the wrong type. Now `_content_type` inference via `mimetypes.guess_type` (fallback `application/octet-stream`), and archives (`.zip`/`.tgz`/`.tar.gz`) additionally get `Content-Disposition: attachment; filename="..."` so clicking the link downloads the file. Motivation: harness-factory v0.11.0's `artifact pack` op — qa-agent now delivers `qa-evidence-<date>.zip` (screenshots + webm recordings + traces) alongside `qa-report.md` in `outputs/`, and the zip link must download cleanly. Non-archive types (md/json/webm/png/html) stay inline. New coverage: `test/unit/test_s3.py` (6 cases: type inference, octet-stream fallback, attachment disposition scope, ExtraArgs/put_object kwargs on all three paths, mocked boto3). |
| v0.43.0 | 2026-08-14 | Sequence pipeline artifact fail-fast: after each step completes, `_run_sequence` verifies the step's declared `type: file` artifact (the v0.37.0 `_artifacts` declarations, previously used only for readback/webhook display) actually exists **non-empty** in `shared_cwd` — a missing or 0-byte deliverable marks the step failed and stops the pipeline, instead of letting downstream steps run against stale or absent inputs. Motivating incident (pipeline 4bdb242d, 2026-08-14): a mesh-remote GDD step "completed" but its file never landed locally (remote fs_write produced a 0-byte file that the L3 relay merged back), the implement step then said "no gdd.md, cannot proceed" — also "completed" — the deploy step deployed nothing, and the QA agent spent 5½ minutes faithfully play-testing an S3 403 error page and published a report about it. Scope deliberately narrow: only `type: file` declarations checked (`type: url` patterns are prefixes with nothing to verify locally), only sequence mode (parallel/race steps have no downstream consumer within the phase), and the check runs after `_exec_step` but before `_webhook_step` so the step_failed webhook fires with the real cause (`deliverable missing: declared artifact 'GDD' (gdd.md) not found (or empty) in <cwd>`). Protected-file change (human-approved): `pipeline.py` — `_check_step_artifact` + `_run_sequence` hook, ~35 lines. New coverage: `test/unit/test_artifact_failfast.py` (7 cases: missing, present, empty file, glob pattern, url skip, no declaration, no shared_cwd). Verified live: a step declaring a file it never wrote fails in seconds with downstream steps still pending. |
| v0.42.0 | 2026-08-11 | True streaming + root fix for the 2026-08-11 "health unreachable" incident. (1) **ACP SDK hotfixes** (new `src/acp_patch.py`, acp-sdk 1.0.3): the SDK's `MemoryStore` shares ONE `asyncio.Event` across all keys, so every store write woke every run watcher, each re-deserializing the full RunData via pydantic (measured: 50 watchers × 100 unrelated writes = 5050 wakeups); worse, `Executor._watch_for_cancellation` only re-checks its `while not task.done()` condition when its inner watch iterator yields, so every finished run left a zombie watcher waking on every write, forever — hours of traffic accumulated hundreds, saturating the main thread at 100% CPU and pushing `/health` to 5-6s. Fixed via the public `create_app(store=...)` hook with `PerKeyEventMemoryStore` (per-key Events in a `WeakValueDictionary`; unrelated writes now wake nothing) plus `apply_executor_patch()` (cancellation watcher cancelled the moment the run task completes). (2) **True streaming**: `_execute_agent_call` buffered every MessagePart and returned the full list only after the agent finished — TTFB equalled total run time (minutes), the whole response sat in memory, and heartbeat parts arrived too late to keep connections alive. Now an async generator yielding parts as they arrive; `CircuitBreaker` gains public `before_call()`/`on_success()`/`on_failure()` (its `call()` refactored onto them, behavior identical) so the streaming path gates admission manually. Success-path output is byte-identical, just incremental. Behavior note: if an agent crashes mid-stream, the client now sees the partial output before the existing fallback banner + replacement agent's answer (previously buffered failures discarded partials); the timeout/rate-limit `_handle_retry` path intentionally stays buffered. Protected-file changes human-approved: `agents.py`, `main.py`. New coverage: `test/unit/test_acp_patch.py` (9 cases: per-key wakeup isolation, watcher GC, zombie reaping, idempotency, manual CB gating). |
| v0.41.0 | 2026-08-11 | Performance: unblock the event loop + wire up data retention, scoped from a three-way audit (hot path / persistence / middleware) with EBS-measured baselines (`busy_timeout` absent = 5.0s loop freeze on writer collision; 30×500KB sync SQLite writes = 171ms max loop stall vs 7ms via thread; `boto3.client()` = 3.2ms per call). External API behavior is byte-identical — every change moves the same call off the loop via `asyncio.to_thread` or caches a pure lookup. (1) **SQLite off the loop**: all store/stats/prompt-log/usage writes reached from async contexts (`agents.py` ×9, `pipeline.py` ×11, `jobs.py` ×9, heartbeat, LiteLLM callback) now run in threads; every jobs.db connection gains `PRAGMA busy_timeout=5000`; usage.db finally gets WAL. Fallback scoring (`get_best_fallback`, 2 queries/candidate exactly during failure storms) threaded at all 4 call sites. (2) **Sync network/file IO off the loop**: ACP `fs/read_text_file`/`fs/write_text_file` auto-replies (reader loop froze on big files) now answer asynchronously from threads; mesh A2A workspace download/upload (`httpx` timeout=120 could freeze the server 2 min); pipeline L3 relay tar-pack/S3/httpx-merge; Discord formatter S3 upload. (3) **Retention wired**: `PipelineStore.delete_old` (now also pruning `conversation_log`), `ChatStore.delete_old`, `PromptStore.cleanup_older_than` (honoring the pre-existing but never-wired `prompt_log.retention_days`), and new `llm_usage` pruning all join the 60s cleanup loop — these tables previously grew forever (data back to 2026-03). New `retention:` config block (pipelines/chat 7d, llm_usage 30d); `completed_at` indexes added for the pruning scans. (4) **Cheap reuse**: LiteLLM proxy reuses one `httpx.AsyncClient` (was per-request); boto3 S3 client cached; template YAML mtime-cached; pipeline prompt files `lru_cache`d. (5) **Task safety**: `create_task` for job/pipeline runs and fs replies now hold strong refs (asyncio keeps only weak ones — untracked tasks can be GC'd mid-flight). Protected-file changes human-approved: `acp_client.py`, `agents.py`, `pipeline.py`, `main.py`. New coverage: `test/unit/test_retention.py` (5 cases). Deferred by design (audit findings for later versions): true streaming in `_execute_agent_call`, pool-lock-free spawn, LiteLLM `stream:true` passthrough, `_event_history` trimming. |
| v0.40.0 | 2026-08-11 | Pool acquire bounded wait: when every slot is busy, `AcpProcessPool.get_or_create()` now polls for a freed slot every 2s for up to `pool.acquire_timeout` seconds (default 60; `0` restores the old immediate-`PoolExhaustedError` behavior) instead of failing on the spot. Data-driven scope: 12 of 769 historical pipelines failed on pool exhaustion (parallel fan-outs hitting `global limit reached (20)` in March, and eight `per-agent limit for opengame (3), all busy` failures on 2026-06-22 that the v0.33.1 pipeline-side retry — 35s total — was too short to absorb), while jobs never hit it; peak observed concurrency is 7, so a full queue system (explicit per-agent queues, workers, priorities, backpressure) was evaluated and rejected as overdesign. The wait loop sits **outside** the pool lock, so acquisitions for other agents proceed while one caller waits, and each retry re-runs the full acquisition path (LRU reuse, eviction, dead-process reaping) so any connection that turns idle is claimed on the next poll; `PoolExhaustedError` after timeout feeds the existing fallback/pipeline-retry paths unchanged. Protected-file changes (human-approved): `acp_client.py` `__init__` + `get_or_create()` (~20 lines), `main.py` one config-read line. New coverage: `test/unit/test_acp_pool.py` (5 cases: wait-then-succeed, bounded timeout, `0` fail-fast, no cross-agent lock blocking, non-pool errors propagate immediately); `test_agents.py` exhaustion test pinned to `acquire_timeout=0`. |
| v0.39.0 | 2026-08-11 | Pipeline artifact delivery + pluggable webhook payload builders + workspace lifecycle. (1) **Artifact delivery**: the final pipeline webhook now carries a `📦 产物` block resolved from the v0.37.0/v0.38.0 `_artifacts` declarations — new async `publish_artifacts()` in `src/render.py` resolves declarations against `shared_cwd` and uploads existing `type: file` artifacts to S3 (boto3 via `asyncio.to_thread`, never blocking the event loop), attaching presigned URLs; without S3 the local path is shown, unmatched patterns render `⚠️ 未生成`, and upload failures degrade silently so notification delivery is never blocked. Line templates under `pipeline.artifact_*` in `default_formatter.yml`. (2) **Payload builders**: the openclaw/generic branching hardcoded in `pipeline._send_webhook()` is now a registry — `get_payload_builder(format)` in `src/formatters.py`, the same pluggable pattern as the job-side `get_formatter(channel)` (closes the 2026-04-20 todo); unknown formats fall back to openclaw and output stays byte-identical to v0.38.0. (3) **Workspace lifecycle**: new `src/workspace.py` sweeper hooked into the main cleanup loop removes expired pipeline workspaces under `{public_workdir}/{mode}/` — `server.workspace_ttl_hours` (default 72, 0 disables), guarded four ways: only `pipeline-*`/`conv-*` named dirs inside the five mode subdirs, running pipelines' cwds skipped (new `PipelineManager.active_cwds()`), expiry requires no file anywhere in the tree newer than the cutoff (directory mtime alone lies when agents write into subdirs), and disk scans throttled to once per 30 min, run via `to_thread`. Protected-file changes (human-approved): `pipeline.py` `_webhook`/`_send_webhook`/`active_cwds()`, `main.py` cleanup-loop hook. New coverage: `test_workspace.py` (8 cases), `test_formatters.py` (6), plus 4 `publish_artifacts` cases in `test_render.py`. |
| v0.38.0 | 2026-08-09 | Extend v0.37.0 artifact declarations to chained pipelines so the Bridge can interpret agent-space `artifacts.json` payloads in full — two of its three presets (`stock-research`, `brainstorm`) declare every artifact inside `context.next.steps[]` rather than top-level `steps`, and v0.37.0 silently discarded those. New `extract_chained_artifacts()` and `propagate_uid()` in `src/render.py` walk the `next` chain to any depth, stripping each `artifact` and stashing the aligned list at `context.next.context._artifacts`. That location is deliberate: `PipelineManager._auto_chain()` builds the downstream pipeline's context as `next_def["context"].copy()`, so the child inherits `_artifacts` and `_uid` as its own and the existing `GET /pipelines/{id}` resolution applies unchanged — no modification to the protected `src/pipeline.py` or `main.py`. Extraction runs after rendering, so chained patterns carry the parent's resolved `{{uid}}`, and the child inherits `shared_cwd` so `type: file` globs hit the same workspace. The submit response reports a `chained_artifacts` count rather than the declarations themselves, since the downstream pipeline id does not exist until the chain fires (it then appears as `next_pipeline_id`). Chain extraction is independent of `input`/`vars`, so pre-rendered payloads benefit too. New coverage: 13 cases in `test/unit/test_render.py` including the three real `artifacts.json` payload shapes and a simulated `_auto_chain` inheritance path. |
| v0.37.0 | 2026-08-09 | Pipeline payload template rendering + artifact declarations, inlined into `POST /pipelines` (no new endpoints). New `input` and `vars` fields enable recursive `{{var}}` substitution across every string under `steps` and `context` — including nested payloads like `context.next.steps[].prompt` — so a UI can post a template plus variables instead of rendering client-side. Scope is flat, lowest to highest precedence: auto `{{uid}}` (8 hex chars from `secrets.token_hex(4)`, identical everywhere in one submission) and `{{date}}`, then `{{input}}`, then `vars`. Unresolved variables return 400 naming each variable and its JSON path — deliberately the opposite of `src/templates.py`, which preserves unknown placeholders: a leftover `{{uid}}` in a pipeline prompt would make the agent really run `aws s3 cp ... s3://bucket/{{uid}}/` and silently create a literal path. New `steps[].artifact` declares expected output (`{"type": "file", "label": "GDD", "pattern": "gdd-{{uid}}.md"}`); the pattern is rendered, stripped from the step before submit, and stashed in `context._artifacts`, with `GET /pipelines/{id}` globbing `type: file` patterns against `shared_cwd` to report `exists`/`path`. Rendering is opt-in — omit `input` and `vars` and the payload is passed through byte-identical to v0.36.2. Protected files `src/pipeline.py` and `main.py` are untouched: the pure renderer lives in new `src/render.py` and all wiring is in `src/routes/pipelines.py`. New coverage: `test/unit/test_render.py` (18 cases across pure functions and route-level HTTP). |
| v0.36.2 | 2026-08-09 | Security: `mesh.token` now fails closed — Bridge refuses to start when `mesh.enabled: true` and the token resolves empty (`src/mesh.py` `resolve_mesh_token()`, mirroring the existing `security.auth_token` guard). `/a2a` and `/a2a/announce` are exempt from the global Bearer middleware by design, so an empty token previously left them reachable with no authentication at all. New SSRF guard (`src/url_safety.py`) validates **client-supplied** outbound URLs — `callback_url` on `POST /jobs` and `workspace_in_url`/`workspace_out_url` on the mesh L3 workspace relay — against loopback/link-local/private/reserved/multicast ranges plus cloud metadata endpoints (`169.254.169.254`, `metadata.google.internal`, Alibaba Cloud's `100.100.100.200`). Validation returns a `SafeTarget` pinned to the exact IP it checked and every real connection goes to that IP with TLS SNI and a full-authority `Host` header, closing the DNS-rebinding window between validation and connect; redirects are explicitly disabled so a `30x` cannot escape the pinned IP. Enforcement lives at send time, so job callbacks are rechecked on every webhook retry and on jobs recovered from the store after a restart. The server-configured `webhook.url` is trusted operator config and stays exempt — a private-address OpenClaw gateway keeps working untouched. `security.allowed_private_targets` (hostname/CIDR list, empty by default) opts specific client-named private targets out of the range checks only; no configuration can ever permit a metadata target. New coverage: route-level HTTP tests for `POST /jobs` and `POST /a2a`/`/a2a/announce`, a simulated DNS-rebinding resolver, and a full-path workspace relay test. Known limitation, documented in [Security → SSRF Protection](docs/security.md#ssrf-protection): `socket.getaddrinfo()` is called synchronously with no explicit timeout, so a slow-resolving hostname can stall the event loop. |
| v0.36.1 | 2026-08-05 | Make `bridge-ctl.sh restart-all` wait up to 30 seconds for LiteLLM's liveliness endpoint before restarting ACP Bridge, replacing the brittle fixed three-second delay and leaving Bridge untouched when LiteLLM never becomes healthy. |
| v0.36.0 | 2026-07-27 | Add optional AWS DevOps Agent support through a fail-closed ACP launcher, without modifying the shared ACP client. The launcher requires explicit operator and AgentSpace selectors, blocks AgentSpace auto-creation unless two switches opt in, and replaces keyword-triggered deep investigations with `/investigate ...` by default. Includes disabled-by-default example configuration, isolated/pinned install guidance, IAM and asynchronous journal limitations, and focused unit coverage. |
| v0.35.3 | 2026-07-27 | Security and ACP lifecycle hardening: refuse startup when the configured Bridge token resolves empty; stop logging token prefixes and credential-bearing AWS SDK request internals; require Bearer auth for download endpoints; restrict the Bearer-exempt LiteLLM callback to loopback; clean canceled/timed-out JSON-RPC futures; terminate removed or partially initialized ACP subprocesses; and propagate ACP prompt errors through retry, circuit breaker, and fallback exactly once. `acp-client.sh --stream` now passes SSE through without buffering the whole response. Placeholder-only agent tests are reported as skipped instead of false passes. |
| v0.35.2 | 2026-07-20 | Cold-start and operations reliability: add unauthenticated `/live` and `/ready` probes; report lazy ACP agents as healthy `cold` instead of down so a fresh restart no longer returns a misleading 503; expose `cold`/`ready`/`down` states in health APIs; schedule self-restarts through transient systemd timers; fix Pipeline integration JSON capture false negatives; skip Docker-only job persistence tests on systemd deployments. |
| v0.35.1 | 2026-07-20 | Cost tracking: add explicit Mantle pricing for `openai.gpt-5.5` ($2.50/M input, $10/M output) and `openai.gpt-5.6-sol` ($5/M input, $30/M output), including `bedrock/` prefix lookup coverage. |
| v0.35.0 | 2026-07-20 | Pipeline resilience: interrupted sequence/parallel/race pipelines recover after Bridge restart with a two-restart limit; local ACP step failures use up to two configured fallback agents (`context.step_fallback=false` disables); lifecycle SSE events persist in SQLite for replay after restart, while high-frequency progress remains memory-only. Conversation pipelines fail cleanly on restart because subprocess session context cannot be reconstructed. |
| v0.34.1 | 2026-07-06 | Version housekeeping: renumber the 2026-06-23 fixes (originally committed as `v0.33.1`, which regressed below the already-released v0.34.0) — heartbeat stale-job filter, thinking idle fix, pool dead-reap, pipeline retry. Sync version across VERSION/README/SKILL.md, backfill missing v0.34.0 changelog entry, reset OPERATIONS.md Part 2 to idle. No code changes. |
| v0.34.0 | 2026-06-22 | Session resume + pipeline inject upstream + heartbeat 活跃度优化. `src/routes/sessions.py` new session-resume endpoints; `acp_client.py`/`agents.py` session resume support; `pipeline.py` inject upstream step output; `default_formatter.yml` heartbeat prompt rework (weaken SILENT rule, encourage proactive short replies). |
| v0.33.0 | 2026-06-15 | Mesh network mode (`private`/`public`/`dual`). Nodes declare their network topology; peers intelligently select private URL (same subnet) or public URL (cross-VPC) for L1/L2 calls. Agent Card now carries `extensions.mesh_mode`/`private_url`/`public_url`. Dual-address self-dedup prevents ghost peer entries. Fully backward-compatible: old nodes without extensions fall back to `card["url"]`. |
| v0.32.0 | 2026-06-08 | Pipeline Rerun: `POST /pipelines/{id}/rerun` — re-execute a completed/failed pipeline reusing its `shared_cwd`. Supports `prompt_override` (prepended as correction to first re-executed step) and `from_step` (partial rerun, skip earlier steps). Returns a new pipeline_id; original unchanged. Mesh L3 transparent — cross-node steps in rerun pipelines trigger standard S3 workspace relay. New integration test `test/integration/test_rerun.sh`. |
| v0.31.2 | 2026-06-06 | Mesh remote agents visible in `/health`, `/agents/details`, and heartbeat snapshot. Previously L2-registered peer agents were callable but invisible to observability surfaces. Now: `EnvCollector` accepts an `_acp_agents_provider` callback to include mesh agents in the heartbeat push (description + domains); `/health` shows them as `mode: "mesh"` entries; `/agents/details` includes full metadata with `mesh.node`/`mesh.peer` fields. Wiring: `main.py` hooks `app.state.acp_agents` into the collector after mesh init. |
| v0.31.1 | 2026-06-06 | `install.sh`: robust update when a modified tracked file blocks fast-forward. The Step 2 update ran `git pull --ff-only 2>/dev/null \|\| warn "git pull failed"` — on existing installs the local (pre-v0.31.0 tracked) `config.yaml` was modified, so `--ff-only` aborted with "local changes would be overwritten", `2>/dev/null` hid the cause, and the update silently no-op'd ("using existing version"). Now: capture and print the real git error, auto-stash local changes (`stash push -u`, includes untracked), retry the fast-forward, and on success tell the user how to recover their stash (`git stash pop`); if it still fails, keep the graceful fallback. New clones won't hit this (config.yaml untracked since v0.31.0), but existing installs with a tracked+modified config.yaml now update cleanly. Installer-only change. |
| v0.31.0 | 2026-06-06 | Mesh remote agents: async-job support + real identity in `/agents`. (1) **Async jobs → mesh**: `JobManager` gets an `_app` ref (wired in `main.py`); `_run` adds a dispatch branch for agents present only in the SDK registry (`app.state.acp_agents`) but neither local nor PTY — they run via new `_run_via_sdk()` (mirrors the proven `mesh_a2a._drain` pattern), so `POST /jobs` to a peer-only agent now works like a local job. (2) **Local vs mesh identity in `/agents`**: previously a peer-only agent showed `description="harness via mesh@http://<ip>"` and all metadata `null` because `record_peer` kept only the skill `id`. Now `PeerInfo` carries `node_name` (parsed from the Agent Card `acp-bridge@<node>`) + full `skill_info` (description/tags); `mesh_client.reconcile()` registers the remote agent with the peer's **real description** + a readable `(via mesh@<node>)` suffix and structured location tags `mesh` / `node:<name>` / `peer:<url>` (built at construction since `Agent.metadata` is read-only). Local agents are symmetrically tagged `local` in `main.py`. Tests: 2 new unit cases in `test_mesh_client.py` + 4 new assertions in `test/integration/test_l2_routing.sh`. |

| v0.30.1 | 2026-06-06 | docs(mesh): 新增 "Two-Node Walkthrough"（双节点从零组网到验证 L0-L3 的端到端步骤 + 排查表）与 "Peer Liveness"（保活机制：announce 周期默认 300s 双向交换 Card 刷新 last_seen，mark_stale 在 last_seen 超过 announce_interval*3≈900s 后标记 unhealthy，unhealthy peer 的远程 agent 从 L2 路由摘除；含 announce_interval 调优建议）。纯文档，无代码改动。 |
| v0.30.0 | 2026-06-06 | install.sh 支持交互式启用 A2A Mesh。安装/更新流程在 S3 步骤后新增 `Enable A2A Mesh? [y/N]`（默认 N，零影响）；启用后询问 node_id（默认 hostname）、self_url（默认本机私网 IP:18010）、seeds（逗号分隔，可空）、mesh token（默认自动生成 / 可粘贴现有 token 加入已有 mesh）。fresh 安装在 config.yaml 的 s3 段后生成 mesh 段；update 安装幂等追加（已有 `mesh:` 则保留）。明文 token 只写入 `.env` 的 `MESH_TOKEN`，config.yaml 永远用 `${MESH_TOKEN}` 变量引用；终端只打印 token 末4位供跨节点核对。纯插入式改动，不触碰原有 token/IP/webhook/S3/agents 逻辑。docs/mesh.md 增加 "Enable via the installer" 说明。 |
| v0.29.2 | 2026-06-06 | Fix ACP usage `input_tokens` normalization + heartbeat newline cleanup. (1) `agents.py` `_record_acp_usage`: `input_tokens` 现在包含 cached tokens（`raw_input + cached`），与 LiteLLM 约定一致（其 input_tokens 含 cached read）；`cached_tokens` 仍单独记录在自己的列，修复 ACP 路径与 LiteLLM 路径统计口径不一致。(2) `heartbeat.py` `clean_response`: 将连续 2+ 换行折叠为 1，清理 heartbeat 输出的多余空行。非破坏性，无 API/handler/路由变化。 |
| v0.29.1 | 2026-06-05 | Fix ACP agent usage recording + enrich heartbeat personalities. (1) `_record_acp_usage`: extract `_meta.quota.model_usage` from ACP `_prompt_result` and write to `usage.db` — fixes gpt-5.5 (Kiro) showing 0 tokens. Covers all call paths: agents.py, main.py heartbeat, pipeline.py, jobs.py. Only records when explicit model name is present to avoid duplicating LiteLLM callback entries. (2) Heartbeat agent personalities rewritten with non-tech backgrounds and hobbies (ex-chef, astronomer, B-boy, Go player, F1 engineer, animator, etc.) for more diverse conversation. (3) heartbeat.py: add `clean_response`/`is_silent` helpers; config.yaml: enable codex heartbeat, route agents through headroom proxy (8787). |
| v0.29.0 | 2026-06-03 | A2A Mesh L3b: multi cross-node pipeline steps + parallel workspace isolation (verification-driven). Added `test/integration/test_l3b_multistep.sh` (2-bridge) covering a sequence chain across two remote steps and a parallel pipeline with two concurrent remote steps in isolated subdirs. Verification confirmed sequence cross-node chaining was already correct, but exposed a real concurrency bug: `_exec_step_remote`'s cleanup deleted the whole `mesh-ws/{pipeline_id}/` S3 prefix in its `finally`, so under parallel the first step to finish wiped other in-flight steps' `in.tgz`/`out.tgz` (404 → lost output). Fix: each step cleans up only its own `step-{idx}/` prefix. `parallel` mode already isolates each step in `shared_cwd/<agent>/`, so concurrent cross-node steps no longer clobber each other. Completes the A2A mesh roadmap (L0 discovery → L1 server → L2 routing → L3 cross-Bridge pipeline). |
| v0.28.0 | 2026-06-03 | A2A Mesh L3a: cross-Bridge pipeline + S3 workspace relay. A pipeline step whose agent lives on a peer (an L2 remote skill) now relays its `shared_cwd` across nodes via S3: the originating node A tars shared_cwd → S3, sends presigned GET/PUT URLs to peer B in the `tasks/send` params, B downloads+unpacks to a temp cwd, runs the agent there, re-packs and uploads, and A merges the result back into the authoritative shared_cwd. New `s3.py` helpers (`presigned_put`/`presigned_get`/`put_bytes`/`delete_prefix`/`pack_dir`/`unpack_dir`); `pipeline.py` `_exec_step` cross-node branch + `_exec_step_remote` (A side); `mesh_a2a.py` `_tasks_send_workspace` (B side, runs via `_call_acp_agent_internal` with the unpacked cwd — pool cwd param, not ACP metadata). S3 is a HARD prerequisite: without it a cross-node step FAILS with a clear error (never silently loses files); local-only pipelines never touch S3. Only A needs S3 write; B uses presigned URLs (no creds). 1-hop limit + local priority carried from L2. L3b (multi cross-node step chains, parallel-mode workspace merge, incremental diff) deferred. |
| v0.27.0 | 2026-06-03 | A2A Mesh L2: A2A Client + mesh routing — completes the mesh core goal (connect to any node, use every node's agents). New `src/mesh_client.py`: `make_a2a_remote_handler` returns a handler matching `make_acp_agent_handler`'s signature that forwards to a peer's L1 `POST /a2a` (`tasks/send`) with `X-A2A-Hop: 1` + mesh.token; `reconcile()` registers a transparent `a2a-remote` handler into `app.state.acp_agents` for every healthy-peer skill this node lacks, reusing the harness route's dynamic-registration pattern. `mesh.py` gains an `on_cycle` hook so each announce cycle reconciles remote handlers; `main.py` wires it and shares a `remote_skills` set with the L1 adapter. 1-hop limit enforced in `mesh_a2a.py`: a hopped inbound request targeting a remote skill is refused (`-32011`). Local agents always take priority and are never shadowed. Routing is transparent to `/runs`/`/jobs`/`/pipelines`. Single-turn only across nodes; cross-node calls remain free. `agents.py` was NOT modified. L3 (cross-Bridge pipeline + S3 artifact) is next. |
| v0.26.0 | 2026-06-03 | A2A Mesh L1: A2A Server (`POST /a2a`) for remote invocation of local agents. New `src/mesh_a2a.py` (`A2AAdapter`) translates A2A JSON-RPC 2.0 `tasks/send` / `tasks/get` into the existing ACP agent handler path — no new execution logic: each call reuses `app.state.acp_agents[skill].run` (with its fallback + circuit-breaker behaviour), so the adapter is a thin protocol shim. `tasks/send` runs an agent synchronously and returns a text artifact; `tasks/get` maps to the local job store. `/a2a` is registered only when `mesh.enabled=true` and authenticates with `mesh.token` (same plane as `/a2a/announce`, exempted from the global Bridge token in `security.py`). Billing stays reserved-but-free: responses carry `metadata.usage`/`metadata.cost` placeholders. `tasks/sendSubscribe` (SSE) and `tasks/cancel` are deferred; L2 mesh routing (calling out to peers) is next. |
| v0.25.1 | 2026-06-03 | Operational cleanup + Bridge orphan detection. `bridge-ctl.sh` adds a safe `orphans` command that reports manually-started Bridge processes outside the `acp-bridge.service` systemd cgroup; `status`, `restart`, and `restart-all` now warn when such processes are present because they can keep port 18010 busy and make systemd restarts ineffective. No automatic process killing. Also marks the superseded v0.24.0 Mesh draft as historical and updates stale TODO entries for the already-shipped SSE `_emitted_at` fix. |
| v0.25.0 | 2026-06-02 | A2A Mesh L0: Agent Card + decentralized discovery. New `src/mesh.py` (`MeshManager` + `PeerInfo`) and `src/routes/mesh.py` expose this Bridge as an A2A node: `GET /.well-known/agent.json` returns a spec-shaped Agent Card whose `skills` are built from local agents via `capability_registry` (tags/version), `POST /a2a/announce` accepts peer broadcasts with separate `mesh.token` auth and replies with this node's card + known peers, and `GET /a2a/peers` shows the peer table behind the global Bridge token. `main.py` wires mesh only when `mesh.enabled=true`; default deployments register no mesh endpoints and start no announce loop. `security.py` exempts only the public Agent Card and peer announce endpoint from global auth; `/a2a/peers` remains protected. Cross-node billing is reserved but inactive: every skill declares `pricing:free` and peer pricing is stored but not used. L0 discovery only — remote invocation remains future L1/L2 work. |
| v0.24.1 | 2026-06-02 | Codex moved from legacy PTY execution to ACP mode through the `@agentclientprotocol/codex-acp` adapter. Codex now participates in session reuse, structured ACP output, and process-pool lifecycle management. `acp_client.py` treats `api-key` authentication as non-fatal when the adapter uses env-based auth. |
| v0.23.1 | 2026-05-29 | Heartbeat diagnostic fidelity + fast-fail probe. `/heartbeat/logs` and `POST /heartbeat/{agent}` now return the real `response` string even when `silent=True`, keeping `silent` as a separate boolean. Heartbeat probe paths use `HEARTBEAT_IDLE_TIMEOUT=30` so stuck agents fail fast instead of holding a pool slot for the default 300s idle timeout. |
| v0.22.0 | 2026-05-27 | Pipeline `step_progress` SSE events — surface intermediate ACP notifications. Previously `pipeline.py:_exec_step_acp` only forwarded `agent_message_chunk` to `step.result`, silently discarding `agent_thought_chunk`, `tool_call`, `tool_call_update`, and `plan` notifications. Long-running agents like opengame appeared dead for minutes between user-visible message chunks. Now: every transformed event emits a `step_progress` SSE event with `kind` ∈ {`tool.start`, `tool.done`, `message.thinking`, `message.part`, `status`} alongside `index` (step or turn), `agent`, and kind-specific fields (`title`/`toolCallId`/`status`/`content`/`text`). `PipelineStep` adds `tools: list[{id,name,status}]` (keyed by `toolCallId`, with orphan-done resilience) and private `_thinking_parts: list[str]`. `to_dict()` exposes `tools` when non-empty. `GET /pipelines/{id}/steps/{i}/live` returns new `thinking` + `tools` fields. Conversation mode also emits `step_progress` per turn. Pure additive: existing `message.part` → `step.result` path preserved; existing SSE event types unchanged; old clients ignore the new event type. Motivated by post-mortem of pipeline `954f7e09` (opengame ran 536s with zero visible progress) |
| v0.21.3 | 2026-05-27 | Persist final prompt for every agent call. New `src/prompt_log.py` (`PromptStore`) records the actual decorated string sent to each agent across all 6 invocation paths (pipeline ACP/PTY/conversation, job ACP/PTY, heartbeat) into a new `prompt_log` table in `data/jobs.db`. Captures `template` (user input), `rendered` (post-`{{var}}` substitution), `final` (with `shared_workspace*.txt` hint + `get_prompt_suffix()`), plus `decorations` list. Default redacts secrets matching OPERATIONS.md patterns; default truncates at 1 MB; best-effort writes never break agent calls. New endpoints: `GET /pipelines/{id}/prompts` / `GET /jobs/{id}/prompts` / `GET /admin/prompts` / `GET /admin/prompts/{record_id}` — all require Bearer token; default response omits the large `final` field, `?include=final` opts in. Config block `prompt_log:` with `enabled` / `redact_secrets` / `max_size` / `retention_days`. Motivated by post-mortem of pipeline `dd0d7ba1` where systemd journal truncated the `session/prompt` log line at 500 chars and the actual prompt couldn't be recovered |
| v0.21.2 | 2026-05-26 | Enable opengame shell execution: `agents.opengame.acp_args` adds `--yolo` flag. Without it, OpenGame CLI runs in headless `auto-edit` mode where shell commands stay disabled — making `npm install` / `npm run build` unreachable, locking opengame into single-file HTML output mode. With `--yolo` opengame can scaffold and build multi-file Phaser projects (its native capability). Verified by inspecting OpenGame's template library: `agent-test/templates/core/` is a full Vite + TypeScript + Phaser 3.90 project skeleton with 5 module families (top_down / platformer / grid_logic / tower_defense / ui_heavy). Config-only change, no code |
| v0.21.1 | 2026-05-26 | SSE event timestamp fix: `_emit_event()` injects `_emitted_at` (unix seconds) into every event data payload. Late-connect history replay now renders correct historical timestamps instead of receive-time. `tools/pipeline-events.sh` prefers `_emitted_at` when present, falls back to current time for backward compatibility. Discovered while diagnosing pipeline `def8aca8` whose 4-minute-old SSE events appeared to happen "just now" |
| v0.21.0 | 2026-05-26 | Live observability for pipelines: (1) **SSE event stream** at `GET /pipelines/{id}/events` — server-sent events emit `pipeline_started`, `step_started`, `step_completed`, `step_failed`, `pipeline_done` with `heartbeat` every 15s; late-connect replays full history then streams live; auto-closes on done. (2) Buffer-poll endpoints `GET /pipelines/{id}/steps/{idx}/live` and `GET /jobs/{id}/live` (uncommitted from previous session, now released). (3) New CLI tools: `tools/pipeline-events.sh` (SSE consumer with human-readable output) and `tools/pipeline-watch.sh` (status-transition poller). (4) `security.rate_limit` / `security.rate_window` config keys (additive). Internal: `Pipeline._event_history` + `_event_subs` set; `_emit_event()` fans out to history list and live `asyncio.Queue`s with backpressure (slow subscribers dropped). Pure-additive — no existing endpoints/behaviors changed |
| v0.20.1 | 2026-05-15 | Internal refactor: deduplicate PTY subprocess logic into `src/utils.py` (shared by agents/jobs/pipeline), extract `_auto_reply()` in acp_client, debounce pid file writes, fix rate limiter memory leak (stale IP purge), reduce LiteLLM startup timeout 15s→5s |
| v0.20.0 | 2026-05-14 | MCP Server: standalone `mcp-server/` directory exposes Bridge agents as MCP tools (stdio transport). Any MCP-compatible client (Claude Desktop, Kiro, Cursor) can call remote agents via `acp_call`, `acp_pipeline`, `acp_submit_job`, `acp_invoke_tool`, etc. Zero changes to Bridge core |
| v0.18.6 | 2026-05-01 | Heartbeat prompt cachePoint: `session_prompt` accepts `list[dict]` (backward-compatible); `build_heartbeat_prompt` returns two Parts — static prefix with `cachePoint: {type: ephemeral}` for LLM KV cache reuse, dynamic suffix as separate Part. Agent spawn supports `env` field passthrough from config.yaml |
| v0.18.0 | 2026-04-25 | Observability & concurrency fixes. New `src/metrics.py`: Prometheus-optional metrics (agent calls, fallback, circuit breaker state, pool connections) with structured-log fallback. `src/rate_limiter.py`: per-agent RPM/TPM sliding-window rate limiting. Fix: `AcpProcessPool.get_or_create()` now uses `asyncio.Lock` to prevent concurrent requests exceeding `max_processes`. Fix: `pipeline._exec_step()` enforces step timeout via `asyncio.wait_for` (previously ignored). Fix: parallel/race pipeline modes no longer mutate shared `pl.context["shared_cwd"]` (race condition). Refactor: `jobs._run_acp()` extracts `_stream_agent()` eliminating 3× duplicate stream loops; `parts.clear()` before retry prevents output mixing. Security: heartbeat prompt no longer leaks absolute project paths (uses basename only); non-heartbeat agents (e.g. kiro) hidden from heartbeat agent list. New `src/stats.py` composite index for query performance. Test suite: 252 pytest + 81 integration |
| v0.17.0 | 2026-04-24 | Agent environment awareness: `src/heartbeat.py` EnvCollector injects online-agent snapshot into prompts (zh/en); `ping_loop` (300s) + `is_agent_healthy()` mark/clear unhealthy agents; unhealthy-mark residual bug fixed (agents removed from pool auto-reset to healthy). Automatic fallback routing in `agents.py`: configurable `FALLBACK_CHAIN` (YAML-persisted, HTTP CRUD), `get_best_fallback()` dual-window scoring (1h base + 15min trend penalty × idle bonus), typed error classification (`AgentTimeoutError`/`AgentRateLimitError`/`AgentModelError` in `src/exceptions.py`), up to 3 retry/fallback attempts with smart retry (timeout→re-call, rate-limit→wait+retry, model-error→next agent). Task complexity prediction (`src/complexity.py`): keyword + length + code-fence heuristic → LIGHT/MEDIUM/HEAVY with timeout map, `should_use_async()` auto-promotes HEAVY to async. Cost estimation (`src/cost.py`): per-model token pricing, CJK-aware token estimator. `tools/fallback_inspector.py` CLI for offline scoring inspection. Test suite expanded to 53 pytest cases: `test_job_fallback` (26), `test_complexity` (10), `test_cost` (10), `test_webhook_sender` (7) |
| v0.16.0 | 2026-04-22 | S3 presigned URL for long job outputs: `src/s3.py` module (startup probe, auto bucket creation, presigned upload), long text → S3 link in Discord formatter (small outputs remain inline), `install.sh` interactive S3 setup, boto3 dependency added. Removed `> ` quote wrapping from Discord output |
| v0.15.20 | 2026-04-21 | Fix Discord thread-create: pass original channel to `WebhookSender.send()` so thread-create fires correctly; use explicit `threadName` + camelCase params |
| v0.15.19 | 2026-04-21 | Fix Discord thread folding: use thread-create instead of thread-reply for long output auto-collapse |
| v0.15.18 | 2026-04-21 | Add OpenClaw (`openclaw acp`) as ACP-mode agent. Bridges stdio JSON-RPC to OpenClaw Gateway over WebSocket. Added to auto_detect, install.sh (with interactive Gateway URL/token config), docs/agents.md |
| v0.15.17 | 2026-04-21 | Fix token inconsistency: install.sh update mode now reads existing token from .env instead of generating a new one (was showing wrong token in OpenClaw skill hint). Client scripts (`acp-client.sh`, `tools-client.sh`, `test/lib.sh`) now accept both `ACP_TOKEN` and `ACP_BRIDGE_TOKEN` (fallback), eliminating the need for manual `export ACP_TOKEN=$ACP_BRIDGE_TOKEN` |
| v0.15.15 | 2026-04-20 | Trae Agent integrated (PTY mode via LiteLLM/Bedrock). Added to auto_detect, install.sh, config, docs/agents.md, test suite. Requires LiteLLM proxy with `additional_drop_params: ["top_p"]` on Anthropic Claude models |
| v0.15.11 | 2026-04-19 | Webhook generic format: `format: "generic"` sends plain JSON POST to any HTTP endpoint (Hermes, custom). Messages auto-chunked at 1800 chars for Discord safety. Enables Hermes Agent as webhook callback target via its webhook adapter (`/webhooks/<route>` + `deliver` to any IM platform). README rewrite: landing page + docs/ split into 12 files. New ASCII banner with agent roster. Startup banner in main.py |
| v0.15.10 | 2026-04-19 | Project positioning: README one-liner and Features section reorganized into three layers (Gateway → Orchestration → IM Workflows). Hermes Agent integrated as ACP agent (8/8 compliance, 7/7 tests). Added to auto_detect, install.sh, config, AGENT.md, SKILL.md, and test suite |
| v0.15.9 | 2026-04-19 | Remove `random` pipeline mode (breaking). Per v0.15.8 `/stats/pipelines` data: 4/8 runs in 7 days with 50% success — lowest usage, lowest success. `/pipelines` now rejects `mode=random`. Dropped from pipeline.py dispatch, routes validator, SKILL.md planner table + decision tree, pipeline.md mode table, README summary table + diagram, test_pipeline.sh case 7. Other 4 modes (sequence/parallel/race/conversation) unchanged |
| v0.15.8 | 2026-04-19 | New `/stats/pipelines?hours=N` endpoint: per-mode aggregation (total / completed / failed / running / avg_duration / max_duration) sourced from existing SQLite `pipelines` table. Enables data-driven decisions about pipeline mode usage |
| v0.15.7 | 2026-04-19 | Observability: `AcpConnection.state` read-only property (`dead`/`busy`/`stale`/`idle`) computed from existing flags; exposed per session in `/health/agents`. Zero-risk additive change, no state-machine refactor |
| v0.15.6 | 2026-04-19 | Cross-service trace propagation: `acp-client.sh` forwards optional `ACP_TRACE_ID` env var as `X-Request-Id` header; OpenClaw/upstream callers can pass their own request id and see it stitched across Bridge logs (including internal httpx/httpcore sub-calls like LiteLLM health probe). SKILL.md Auth section documents the pattern |
| v0.15.5 | 2026-04-19 | Observability: (1) request trace_id — every HTTP request gets an `X-Request-Id` (accepts client-supplied, else generates 12-char hex); id propagates via contextvar through async logs (including third-party httpcore/uvicorn) and echoes in response header; (2) `/stats.tools_used` now aggregates by tool category (`Running`/`Reading`/`Editing`/...) instead of full command string, making the field actually useful |
| v0.15.4 | 2026-04-19 | Robustness: readline now tolerates `LimitOverrunError` (>64KB single line no longer kills connections); PTY agents gain `max_duration` hard cap (default 600s) to prevent runaway subprocesses whose output dribbles in below `idle_timeout`; README gains a `⚠️ Security Considerations` section covering the token-as-shell risk, `--trust-all-tools` implications, prompt injection, and recommended deployment shapes |
| v0.15.3 | 2026-04-18 | `install.sh` auto-installs systemd units before startup: generates `/etc/systemd/system/acp-bridge.service` (and `litellm.service` when Codex/Qwen is enabled), `daemon-reload` + `enable`, then starts via `systemctl start` instead of `start.sh` |
| v0.15.2 | 2026-04-18 | `install.sh` prints OpenClaw skill setup box after install: skill URL + `ACP_TOKEN` + `ACP_BRIDGE_URL` (detects LAN/WAN IP). README install section updated with matching guidance |
| v0.15.1 | 2026-04-18 | Session reuse per (agent, channel_id) to avoid spawning new CLI per Discord message; `POST /harness` supports `model` field to override auto model selection; Job formatters (Discord/Feishu/Fallback) now read from `default_formatter.yml` |
| v0.15.0 | 2026-04-18 | Pluggable message template system: all webhook/status messages (pipeline, job, agent) defined in `src/templates/default_formatter.yml`. Change message format by editing YAML only — no code changes needed. `PipelineFormatter` + `fmt()` helper replace hardcoded strings in pipeline.py and agents.py |
| v0.14.6 | 2026-04-18 | systemd lifecycle management + `bridge-ctl.sh` self-bootstrap script. Kiro agents inside Bridge can now safely restart Bridge via `./bridge-ctl.sh restart` (systemd-backed). OPERATIONS.md updated: Phase 0 gains bridge-ctl rules, Phase 6 uses systemd instead of Docker as primary restart method |
| v0.14.5 | 2026-04-18 | Enhanced `GET /health`: three-state status (ok/degraded/unhealthy), process pool watermark, system memory usage, per-agent summary, async job queue summary, LiteLLM dependency status, human-readable uptime |
| v0.14.4 | 2026-04-17 | SKILL planner: Step 2 gains a **capability–task gate** — presets with `Write? = no` must not be paired with persistence verbs (write/save/produce doc/create md/...); Step 3 makes this gate a hard stop before showing the plan card. Troubleshooting gains the recurring 0-byte-artifact symptom (incident `493987b9` PM/QA text-stranded PRD + report) |
| v0.14.3 | 2026-04-17 | SKILL planner: dependency-first mode selection. New mandatory `Task dependency` row in Step 3 decision summary (sequential / independent / shared); Step 5 cheatsheet prepended with a decision tree driven by dependency instead of user phrasing. Orchestration templates gain a `Dep.` column. Anti-pattern noted: don't pick `sequence` just because user said "先…再…" when tasks are actually independent |
| v0.14.2 | 2026-04-17 | Capture harness-factory 0.8.0's `activated.resolvedModel` on `session/new`; `GET /harness` response now includes `resolved_model` (the actual model picked, populated after first call). Backward compatible with older harness-factory and non-harness ACP agents |
| v0.14.1 | 2026-04-17 | SKILL.md Step 4 rules: (1) harness-creation echo must include `preset` + `model`; (2) pipeline/conversation results must append a duration breakdown (per-agent + total) with `stop_reason` for conversation mode |
| v0.14.0 | 2026-04-17 | SKILL.md structural rewrite for token efficiency: front-load Planning Workflow (hot path now in first ~90 lines vs. mid-doc previously); Preset capability matrix moved adjacent to Planning; compress Commands (17→1 line), Harness usage (17→5 lines), Output Rules (20→4 lines). 307 → 228 lines, no semantic changes |
| v0.13.7 | 2026-04-17 | Enrich SKILL.md preset table with `Write?` column (authoritative from `harness-factory -dry-run`) and `Recommended model` binding; warn `reviewer` must output via text reply (real incident: pipeline be8e1d8c hit `[loop detected: fs_read]` with no write target) |
| v0.13.6 | 2026-04-17 | New `skill/references/orchestration-patterns.md`: 10 preset templates for 2/3/4-5 agent orchestration (relay, dual-view, debate-2, race-2, review-write-test, fan-out-merge, roundtable-3, staged-pipeline, parallel-then-judge, dual-debate-then-judge). SKILL.md Step 5 points at it; >5 agents left to host LLM |
| v0.13.5 | 2026-04-17 | Skill doc hardening: dedupe pipeline content (single source = `pipeline.md`); add 3 real-bug troubleshooting rows (permission schema, PTY idle timeout, harness spawn vs. call); Planner Step 4.1 fallback guidance on failures; `CLAUDE_SKILL_DIR` host-injection note + fallback expansion |
| v0.13.4 | 2026-04-17 | Translate `skill/SKILL.md` Planner Workflow (Step 1–7), decision-summary card, duration estimation guide, preset-intent mapping, and frontmatter description to English. No behavior change; 2 intentional CJK confirmation aliases kept. |
| v0.13.3 | 2026-04-17 | Fix auto-allow permission response shape to match ACP 1.0 RequestPermissionOutcome schema (wrap in `outcome: {outcome: "selected", optionId}`); previously claude-agent-acp 0.20+ treated the old flat form as reject. Also: `start.sh` gains `--stop` / `--foreground` / `--restart` and default background mode with `/health` readiness wait |
| v0.13.2 | 2026-04-17 | Planner rules: enforce async for >60s tasks, add duration estimation guide; require full job_id/pipeline_id in response relay |
| v0.13.1 | 2026-04-17 | Planner enhancements (agent sourcing + decision-rich plan card) + skill dir reorg: scripts/ and references/ subdirs, removed stale archive |
| v0.13.0 | 2026-04-17 | Skill-side planner workflow: classify intent → fetch /agents → show plan table → `yes` to execute; adds clarification heuristics and common intent lookup |
| v0.12.3 | 2026-04-17 | Fix test_kiro.sh async-submit assertion to match client output (Submitted/已提交/job:) |
| v0.12.2 | 2026-04-16 | Smart install.sh: state detection (first run vs update), install missing agent CLIs, incremental config.yaml update, Node.js detection |
| v0.12.1 | 2026-04-16 | Interactive install.sh: agent selection, harness-factory fallback, token setup, config generation |
| v0.12.0 | 2026-04-16 | Zero-config auto-detect mode: no config.yaml needed, auto-discovers agent CLIs in PATH; one-line install.sh |
| v0.11.6 | 2026-04-14 | Fix dynamic harness cross-agent process reuse; fix card mode empty output (bash read merging tabs) |
| v0.11.5 | 2026-04-13 | Dynamic Harness API: runtime creation of harness-factory agents via HTTP (POST/GET/DELETE /harness) |
| v0.11.4 | 2026-04-13 | Fix health_check killing busy agent connections |
| v0.11.3 | 2026-04-12 | Prompt templates: reusable prompt definitions with variable rendering |
| v0.11.2 | 2026-04-12 | Agent call statistics API (GET /stats) |
| v0.11.1 | 2026-04-13 | Harness Factory integration: profile-driven lightweight agents |
| v0.11.0 | 2026-04-11 | Harness Factory support: profile-driven ACP agents via harness-factory binary |
| v0.10.13 | 2026-04-11 | Session close API (DELETE /sessions/{agent}/{session_id}) |
| v0.10.12 | 2026-04-09 | Pipeline conversation mode improvements |
| v0.10.11 | 2026-04-03 | OOM protection: memory-aware pool eviction，内存超 80% 自动驱逐空闲连接，降低默认池上限 |
| v0.10.10 | 2026-04-03 | Shared workspace for all pipeline modes: sequence/parallel/race/random 共享工作目录，agent 间文件可见 |
| v0.10.8 | 2026-04-02 | Conversation 修复增强：中文自动检测、每个 agent 首轮注入 topic、prompt 文件化管理、共享工作目录、transcript 空输出 fallback、webhook 完整内容分段推送 |
| v0.10.7 | 2026-04-02 | Pipeline conversation mode: 多 agent 多轮对话，Bridge 只传话 + SQLite 记录，支持 @mention 定向调度 |
| v0.10.6 | 2026-04-02 | Pipeline random mode: 从 steps 中随机选一个 agent 执行，其余跳过 |
| v0.10.5 | 2026-04-01 | Pipeline PTY support: Codex 等 PTY agent 可参与 pipeline (sequence/parallel/race)，.env auto-loader 统一环境变量管理 |
| v0.10.4 | 2026-04-01 | Connection pool LRU eviction: 满载时淘汰最不活跃的空闲连接，同 agent 优先复用进程，消灭 pool_exhausted |
| v0.10.3 | 2026-04-01 | Pipeline per-step webhook push: sequence逐步推送, parallel完成即推, race赢家即推, 最终推汇总耗时 |
| v0.10.2 | 2026-03-31 | Pipeline SQLite persistence — survives restarts, queryable via API |
| v0.10.1 | 2026-03-31 | Pipeline session_id fix, skill pipeline recognition rules |
| v0.10.0 | 2026-03-31 | Multi-agent pipeline: sequence, parallel, race execution (`POST /pipelines`) |
| v0.9.6 | 2026-03-31 | Webhook retry limit (max 5) + response body logging on failure |
| v0.9.5 | 2026-03-29 | File upload API: `POST /files` with configurable `upload_dir`, skill `--upload`, Web UI attach button |
| v0.9.2 | 2026-03-29 | AGENT.md rewrite: agent-first flow with clear 🤖/👤 boundaries |
| v0.9.1 | 2026-03-29 | README refresh: fix ASCII art arrow direction, add Web UI + direct HTTP path, remove screenshot |
| v0.9.0 | 2026-03-28 | Adaptive Web UI: native CSS variables, dark mode, responsive layout, drop jQuery + Tailwind |
| v0.8.4 | 2026-03-28 | Qwen Code + OpenCode agent support, test_qwen.sh, test_opencode.sh |
| v0.8.3 | 2026-03-21 | Web UI opt-in (`--ui`), chat persistence (SQLite), message folding, settings panel |
| v0.8.1 | 2026-03-19 | Docker image 618→439MB, agent-optimized README, test coverage 25→31 |
| v0.8.0 | 2026-03-19 | Docker light mode: gateway-only image with host agent mounting |
| v0.7.3 | 2026-03-18 | Request-level cwd, tools proxy fix, test improvements |
| v0.7.2 | 2026-03-18 | Multi-IM formatter (Discord/Feishu), unified target field, systemd |
| v0.7.1 | 2026-03-18 | ACP agent compliance test, AGENT_SPEC.md, echo-agent reference impl |
| v0.7.0 | 2026-03-17 | OpenClaw tools proxy, agent health probe, auto-recovery |
| v0.6.0 | 2026-03-15 | Codex PTY support, LiteLLM integration, acp-client.sh |
